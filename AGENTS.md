# Frontend Guide

이 파일은 `frontend/` 아래에 적용하며 루트 `AGENTS.md`를 보완한다.

## 화면과 역할

- 참가자용 모바일 화면, 진행자 제어 화면, 16:9 공용 화면의 요구를 구분한다. 한 컴포넌트에 세 역할의 권한과 레이아웃을 조건문으로 과도하게 섞지 않는다.
- 참가자 핵심 흐름은 QR/방 코드 입장 -> 닉네임 -> 문장 3개 제출 -> 대기 -> 투표 -> 라운드 결과 -> 최종 순위다.
- 진행자 핵심 흐름은 방 생성 -> QR 대기실/제출 현황 -> 게임 시작 -> 투표 제어 -> 결과 공개 -> 다음 라운드 -> 최종 순위다.
- 공용 화면은 멀리서 읽히는 발표자, 문장, 타이머, 투표 완료 인원과 결과에 집중한다. 진행자 토큰이나 민감한 조작 UI를 프로젝터 화면에 노출하지 않는다.

## 상태 관리와 실시간 동작

- 서버 스냅샷을 권위 있는 상태로 사용한다. 클라이언트가 상태 전환, 점수, 마감 성공을 선반영해 확정하지 않는다.
- 실시간 이벤트는 캐시를 갱신하는 신호로 다룬다. 연결 직후, 재연결 후, 이벤트 버전 누락 또는 순서 역전이 감지되면 서버 스냅샷으로 재동기화한다.
- 연결 끊김을 눈에 띄게 알리고 지수 백오프로 자동 재연결한다. 실시간 연결이 실패하면 서버가 지원하는 범위에서 폴링으로 대체하며 중복 구독을 만들지 않는다.
- 남은 시간은 서버의 `voting_ends_at`을 표시한 값일 뿐이다. 로컬 타이머가 0이 되어도 결과를 임의 확정하지 않고 서버의 마감 상태를 기다린다.
- 투표 중에는 마지막 서버 승인 선택을 복구한다. 제출 중 상태를 표시하고, 마감 경계에서 거절되면 선택이 집계되었다고 오인시키지 않는다.
- 새로고침 후 참가자/진행자 세션과 현재 화면을 복구한다. 비밀 토큰을 URL, 분석 이벤트, 오류 로그 또는 접근 가능한 전역 상태에 복사하지 않는다.

## 입력과 정보 공개

- 문장 폼은 정확히 진짜 2개와 가짜 1개, 문장별 5~100자, 공백 금지, 동일 문장 금지를 즉시 안내하되 서버 오류를 최종 결과로 처리한다.
- 자동 금칙어 필터나 진위 판정 기능을 추가하지 않는다. 민감한 개인정보를 적지 말라는 안내를 제출 전에 제공한다.
- 사용자 문장은 프레임워크의 안전한 텍스트 렌더링을 사용한다. 임의 HTML 삽입 API를 사용하지 않으며 필요 시 검증된 sanitizer와 명시적 테스트를 둔다.
- 정답 공개 전에는 정답 필드가 UI 상태, 개발용 디버그 출력, 사전 로딩 데이터에 존재한다고 가정하지 않는다. 숨김 CSS로만 감추는 구현은 금지한다.
- 익명 투표가 꺼져 있더라도 투표자 목록은 결과 공개 후에만 표시한다. 진행 중에는 완료 인원과 투표 가능 인원만 표시한다.

## 반응형과 접근성

- 참가자 흐름은 360px 너비부터 가로 스크롤 없이 완료할 수 있게 한다. 터치 대상은 충분히 크게 만들고 모바일 Safari의 키보드, safe area, viewport 변화를 점검한다.
- 공용 화면은 16:9에서 핵심 정보의 시각적 우선순위와 거리 가독성을 유지한다. 긴 닉네임과 100자 문장에서도 레이아웃이 무너지지 않게 한다.
- 색만으로 선택, 진짜/가짜, 성공/오류, 연결 상태를 구분하지 않는다. 텍스트나 아이콘과 접근 가능한 이름을 함께 제공한다.
- 모든 주요 동작은 키보드로 가능해야 하며 논리적인 포커스 순서와 명확한 포커스 표시를 유지한다. 상태 변경과 오류는 적절한 live region으로 전달하되 과도한 반복 안내를 피한다.
- 핵심 텍스트는 WCAG AA 명암비를 충족하고, 브라우저 확대와 글자 크기 확대 시에도 게임 진행이 가능해야 한다.
- 최신 2개 버전의 Chrome, Safari, Edge를 대상으로 하며 QR 접속부터 최종 결과까지 모바일 Safari와 Android Chrome을 우선 검증한다.

## 오류와 사용자 안내

- 만료/없는 방, 정원 초과, 중복 닉네임, 시작 후 입장, 잘못된 제출, 권한 없음, 투표 마감, 네트워크 단절을 서로 구분되는 한국어 안내와 복구 동작으로 표시한다.
- 오류는 사용자가 다음에 할 수 있는 행동을 함께 제시한다. 재시도가 안전하지 않은 명령은 무조건 자동 재시도하지 않는다.
- 버튼 연속 클릭을 막되 서버 멱등성의 대체 수단으로 여기지 않는다. 처리 중, 성공, 실패 상태를 분명하게 표시한다.
- 발표자 이탈, 진행자 재접속, 일시 정지 상태에서는 현재 게임이 유지되고 있음을 설명하며 허용되지 않은 조작을 비활성화한다.

## 프론트엔드 테스트 기준

- 역할별 핵심 화면과 상태를 컴포넌트 테스트하고, API 성공뿐 아니라 도메인 오류와 연결 끊김을 검증한다.
- 방 생성부터 최종 순위까지 진행자·참가자 다중 세션 E2E 흐름을 유지한다. 발표자 투표 불가, 투표 변경, 마감 직후 거절, 새로고침 복구를 포함한다.
- 360px 모바일과 16:9 공용 화면을 시각적으로 점검하고, 키보드 탐색, 포커스, accessible name, 명암비를 자동/수동 접근성 검사에 포함한다.
- 서버 응답이나 이벤트가 늦거나 중복·역순으로 도착하는 테스트를 추가해 화면이 이전 상태로 되돌아가거나 점수를 중복 표시하지 않는지 확인한다.
