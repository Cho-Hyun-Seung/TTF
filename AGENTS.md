# TTF Repository Guide

## 적용 범위와 기준 문서

- 이 파일은 저장소 전체에 적용한다. `backend/AGENTS.md`와 `frontend/AGENTS.md`는 각 디렉터리에서 이 규칙을 보완한다.
- 제품 동작의 기준은 루트의 `platformprd.md`다. 기능을 구현하거나 동작을 바꾸기 전에 관련 절과 수용 기준, QA 시나리오를 확인한다.
- PRD 안에서 내용이 충돌하면 22장 "남은 의사결정"에 적힌 확정 답변을 우선하고, 그다음 MVP 수용 기준, P0 요구사항 순으로 따른다.
- 기술 스택은 아직 확정되지 않았다. 기존 설정과 패턴이 생기면 이를 우선하며, 새 프레임워크·데이터베이스·상태 관리 도구를 임의로 추가하지 않는다. 큰 선택이 필요하면 근거와 대안을 먼저 기록한다.

## 제품 원칙

- TTF는 설치와 회원가입 없이 QR 또는 6자리 방 코드로 참여하는 실시간 진진가 게임이다.
- 진행자 권한은 방 코드와 분리된 추측 불가능한 자격 증명으로 보호한다. 방 코드나 참가 링크만으로 진행자 동작을 허용하지 않는다.
- 게임 상태, 투표 마감, 점수, 라운드 순서, 문장 공개 순서는 서버가 결정한다. 클라이언트 상태를 권위 있는 값으로 신뢰하지 않는다.
- 가짜 문장 정보와 개별 투표 정보는 허용된 공개 시점 전에는 API, 실시간 이벤트, 로그, 분석 이벤트 어디에도 노출하지 않는다.
- 확정된 MVP 점수는 가짜 문장을 맞힌 참가자에게 주는 1점뿐이다. 발표자 기만 점수와 무득표 보너스는 구현하지 않는다. 동점자는 공동 순위로 처리한다.
- 게임 시작 후 신규 입장은 허용하지 않는다. MVP 관전자 모드를 만들지 않는다.
- 데이터는 활성 게임과 재접속 복구에 필요한 동안만 유지한다. 종료·취소·만료된 게임의 기록 보관이나 게임 히스토리를 만들지 말고, 런타임 정리 정책에 따라 제거한다.
- 자동 금칙어 필터는 적용하지 않는다. 대신 길이·형식 검증, 공백/중복 검사와 안전한 출력 처리는 반드시 적용한다.
- QR에는 권한 토큰이나 개인 정보를 넣지 않고 방 코드가 포함된 참가 URL만 넣는다.
- MVP는 한 방 최대 100명을 지원해야 하며, 상태 전파 p95 1초, 일반 동작 p95 500ms, 투표 마감 후 집계 2초 이내를 목표로 한다.

## 상태와 계약

- 정상 상태 흐름은 `LOBBY -> SUBMISSION -> READY -> ROUND_INTRO -> VOTING -> VOTE_CLOSED -> RESULT -> ... -> FINISHED`다. `PAUSED`, `CANCELLED`, `EXPIRED` 예외 상태를 포함해 모든 전환을 명시적으로 검증한다.
- HTTP API와 실시간 이벤트의 이름 및 의미는 PRD 13장을 출발점으로 삼는다. 계약을 변경하면 생산자와 소비자, 타입/스키마, 테스트와 관련 문서를 한 작업에서 함께 갱신한다.
- 네트워크 재시도, 중복 클릭, 여러 탭을 정상 상황으로 취급한다. 쓰기 요청은 가능한 한 멱등적으로 설계하고, 투표와 상태 전환의 경합은 원자적으로 처리한다.
- 실시간 이벤트는 최소 데이터만 알리며, 연결 또는 이벤트 순서가 끊기면 전체 서버 스냅샷을 다시 조회해 복구할 수 있어야 한다.

## 작업 원칙

- P0 핵심 흐름을 먼저 완성한다. 요청받지 않은 P1/P2 기능, 관전자 모드, 계정, 게임 기록, 복잡한 점수 옵션을 선행 구현하지 않는다.
- 변경은 작고 역할이 분명하게 유지한다. 공통 계약은 한 곳에서 정의해 프론트엔드와 백엔드에 서로 다른 상수나 상태 이름을 중복 작성하지 않는다.
- 입력 검증은 사용자 안내를 위한 클라이언트 검증과 보안을 위한 서버 검증을 모두 둔다. 모든 출력은 렌더링 맥락에 맞게 안전하게 처리한다.
- 원문 문장, 세션 토큰, 진행자 토큰을 로그나 분석 이벤트에 기록하지 않는다. 오류 메시지에도 비밀값을 포함하지 않는다.
- 의존성을 추가하기 전에 표준 기능이나 기존 의존성으로 해결 가능한지 확인한다. 추가했다면 목적, 런타임 영향, 라이선스 및 잠금 파일 변경을 검토한다.
- 사용자에게 보이는 기본 문구는 자연스럽고 간결한 한국어로 작성한다. 내부 상태명과 API 필드는 일관된 영어를 사용한다.

## 검증과 완료 기준

- 새 동작에는 정상 경로뿐 아니라 권한 실패, 잘못된 상태 전환, 중복 요청, 마감 경합, 재연결을 다루는 테스트를 추가한다.
- 점수 계산과 상태 머신은 결정적인 단위 테스트를 갖추고, 방 생성부터 최종 순위까지 핵심 흐름은 통합 또는 E2E 테스트로 보호한다.
- 3명 흐름을 기본 기능 검증에 사용하고, 30명과 100명은 동시성·성능 검증에 포함한다.
- 변경한 영역의 포맷, 정적 검사, 타입 검사, 테스트를 실행한다. 전체 검증을 실행할 수 없다면 실행한 범위와 남은 위험을 작업 결과에 명시한다.
- 사용자 동작이나 계약이 바뀌면 `platformprd.md` 또는 별도 설계 문서도 함께 갱신하되, 승인되지 않은 제품 결정을 문서 변경으로 확정하지 않는다.

